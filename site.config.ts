export const siteName = 'Schob Digital';

// Update this if the site later moves from GitHub Pages to a custom domain.
export const siteOrigin = 'https://schob-digital.github.io';
export const siteBasePath = '/schob.digital/';
export const siteBaseUrl = new URL(siteBasePath, siteOrigin).toString();
